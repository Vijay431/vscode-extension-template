# Patterns excluded from commit/PR size checks (grep -E format, one per line).
# Lines starting with # and blank lines are ignored.
# Both scripts/check-commit-size.sh and .github/workflows/pr-commit-size.yml
# read this file so the exclusion list stays in one place.

pnpm-lock\.yaml$
package-lock\.json$
yarn\.lock$
